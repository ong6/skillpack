interface:
  display_name: "Wait, What?"
  short_description: "Re-pitch an answer that did not land"
  default_prompt: "Use $wait-what to re-pitch the last answer in plain English."

policy:
  allow_implicit_invocation: false
